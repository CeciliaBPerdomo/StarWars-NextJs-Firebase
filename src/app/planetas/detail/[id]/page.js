function DetallePersonajes({params}) {
    const {id} = params

    return (
        <div>
            hola {id}
            {/* <PersonajeIndividual id={id} /> */}
        </div>
    )
}

export default DetallePersonajes